packages:
  - "apps/*"
  - "packages/*"
# pnpm 11 defaults to 1440m; keep 0 so fresh lockfile entries from
# `pnpm update --latest` remain installable in CI without waiting a day.
minimumReleaseAge: 0
# pnpm 10+ requires injected workspace deps for `pnpm deploy`; keep legacy
# deploy for desktop sidecar + Console local-host packaging.
forceLegacyDeploy: true
# Avoid TTY prompt when install switches away from deploy --prod state.
confirmModulesPurge: false
# GHSA-2v37-7h3g-55p8: vite pins postcss@8.5.25 → nanoid@3.3.16;
# 8.5.26 bumps nanoid to ^3.3.17 (vite already allows ^8.5.25).
overrides:
  postcss: ">=8.5.26"
  # GHSA-f65p-4m7j-42xc / GHSA-fph4-wmhf-6fwf / GHSA-jqff-g426-hqxp: fast-uri <3.1.6 (high, via commitlint→ajv)
  fast-uri: ">=3.1.6"
  # GHSA-x5fp-wj9c-mxmx / GHSA-4mjr-xmp4-gh2g: qs <6.16.0 (moderate, via express@5.2.1)
  qs: ">=6.16.0"
allowBuilds:
  "@julusian/midi": true
  esbuild: true
  gl: true
# eslint-plugin-react@7 peers ESLint ≤9; flat config still works on ESLint 10.
peerDependencyRules:
  allowedVersions:
    "eslint-plugin-react>eslint": "10"
