{
  "name": "stagesync",
  "version": "5.4.12",
  "private": true,
  "license": "BUSL-1.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm generate:map && turbo run dev --filter=@stagesync/web --filter=@stagesync/server --filter=@stagesync/shared",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "format": "prettier --write \"**/*.{ts,tsx,md}\" \"!docs/REPO_MAP.md\"",
    "check-types": "tsc -p scripts/tsconfig.json && turbo run check-types",
    "check-types:tests": "tsc -p scripts/tsconfig.json && turbo run check-types:tests",
    "check-types:all": "tsc -p scripts/tsconfig.json && turbo run check-types:all",
    "sync-version": "node scripts/release/sync-version.mjs",
    "cut-release": "node scripts/release/cut-release.mjs",
    "sync:launcher-ui": "node apps/desktop/scripts/sync-launcher-ui.mjs",
    "lint:ss-css": "node scripts/quality/lint-ss-css.mjs",
    "lint:knip": "knip --exclude files,dependencies,devDependencies,unlisted,binaries,exports,types,nsExports,nsTypes,enumMembers,namespaceMembers,catalog,catalogReferences",
    "benchmark:record": "npx tsx apps/web/scripts/benchmark/record-benchmark.ts",
    "prepare": "node -e \"try{require('child_process').execSync('husky',{stdio:'inherit'})}catch{}\"",
    "generate:map": "node scripts/quality/generate-repo-map.mjs",
    "dev:hub": "tsx --no-warnings scripts/hub/dev-hub.ts",
    "clean:android": "cd apps/console/android && ./gradlew clean && cd ../../../apps/performer/android && ./gradlew clean",
    "clean": "pnpm run clean:android"
  },
  "devDependencies": {
    "@clack/prompts": "^1.7.0",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@types/node": "^26.2.0",
    "@types/qrcode-terminal": "^0.12.2",
    "@vitest/coverage-v8": "^4.1.10",
    "husky": "^9.1.7",
    "knip": "^6.32.2",
    "picocolors": "^1.1.1",
    "prettier": "^3.9.6",
    "qrcode-terminal": "^0.12.0",
    "tsx": "^4.23.12",
    "turbo": "^2.10.9",
    "typescript": "7.0.2"
  },
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=22"
  }
}

